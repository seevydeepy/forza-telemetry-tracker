<!-- OKF-ROUTING:START -->
## OKF Routing

At the start of substantive repository work, if `docs/solutions.manifest.json` exists, use `$okf-router` to select the relevant OKF route card before broad source inspection. If the skill is unavailable, manually inspect the manifest and read only the matched `docs/okf/<id>/routing_guidance.card`, then `solution.md` if needed.

At the end of substantive code, config, tooling, ownership, or routing changes, use `$okf-archivist` to optimise OKF route cards or docs from the turn evidence.
<!-- OKF-ROUTING:END -->
